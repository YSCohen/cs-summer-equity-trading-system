---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/apps

patches:
  - path: ingress-overwrite.yaml
  - target:
      group: traefik.io
      version: v1alpha1
      kind: Middleware
      name: streamlit-fallback
    patch: |-
      $patch: delete
