---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: data
resources:
  - cluster.yaml
  - pooler-ro.yaml
  - pooler.yaml
  - sync-secret-job.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: postgres-init-schema
    files:
      - 01-schema.sql=../../../../../db/init.sql
