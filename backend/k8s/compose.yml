---
services:
  toolbox:
    build:
      context: .
      dockerfile: Dockerfile.toolbox
    container_name: k8s-toolbox
    network_mode: host
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ../../:/workspace
