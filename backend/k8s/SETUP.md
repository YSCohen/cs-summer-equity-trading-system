# Required Tools

- Docker
- kubectl
- k3d CLI Tool

## Installation of tools

Right now we are doing everything through docker.
Just use the `make cluster-up`
I will probably make another make command like `make up`
