[project]
name = "load-test"
version = "0.1.0"
description = "This project will load test the FastAPI Endpoints"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "locust>=2.44.4",
]
